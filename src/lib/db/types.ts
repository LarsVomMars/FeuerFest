export enum Role {
    OWNER = "OWNER",
    ADMIN = "ADMIN",
    USER = "USER",
}

export const RoleValue = {
    USER: 0,
    ADMIN: 1,
    OWNER: 2,
};

export enum Status {
    PENDING = "PENDING",
    ACTIVE = "ACTIVE",
    INACTIVE = "INACTIVE",
}

export enum ProductType {
    FOOD = "FOOD",
    DRINK = "DRINK",
    BAR = "BAR",
}
